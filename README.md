# SELINA-reference

#### Reference models of SELINA

- The models that can be loaded in python are stored under `python` folder and the models that can be loaded in R are stored under `r`
- The models used to predict for normal data are under `Normal` folder and the models used to predict for disease data are under `Panimmune` folder.
